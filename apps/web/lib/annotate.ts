/**
 * Compatibility entry point for the web editor. The drawing model is shared
 * with the native Mac editor so both surfaces flatten the exact same marks.
 */
export * from "@capso/shared/annotate";
