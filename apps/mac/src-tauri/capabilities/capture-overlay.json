{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture-overlay",
  "description": "Scoped interaction access for the non-activating capture overlay",
  "windows": ["capture-overlay"],
  "permissions": ["core:event:default"]
}
