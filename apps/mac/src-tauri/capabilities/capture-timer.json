{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture-timer",
  "description": "Scoped event access for Capso's visible capture timer",
  "windows": ["capture-timer"],
  "permissions": ["core:event:default"]
}
